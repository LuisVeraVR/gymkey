DATABASE_URL="postgresql://user:password@localhost:5432/gymkey"
JWT_SECRET=change_me_in_production
PORT=3001
# Si cambias PORT, en apps/mobile crea .env con el mismo valor, p. ej.:
# EXPO_PUBLIC_API_PORT=3333
# Comma-separated origins for the admin web (Next.js default dev port is 3000)
CORS_ORIGIN=http://localhost:3000
